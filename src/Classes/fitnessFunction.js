// Главная функция для подсчета фитнеса конкретного претендента // потомка

import { params } from "../params.js";

function calculateFitness(applicant) {
    
    let fitness = 0;
    let owerstack = new Array(params.numberSkills).fill(0); // переработка
    // обход массива зарплат
    params.applicantsArray.salary.array.forEach((salar, j) => {
        
    });
    
    return fitness;
  }
  
  export default calculateFitness;
  